# import AbilityObj, StatObj, SpriteObj from "pokemonAttributes.graphql"

type Pokemon {
    id: Int
    name: String
    height: Int
    abilities: [AbilityObj]
    stats: [StatObj]
    sprites: SpriteObj
}