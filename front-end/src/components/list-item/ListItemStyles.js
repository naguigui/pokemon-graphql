import { StyleSheet } from "react-native";

export default StyleSheet.create({
  ListItem: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center"
  }
});
