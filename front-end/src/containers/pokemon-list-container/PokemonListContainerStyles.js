import { StyleSheet } from "react-native";

export default StyleSheet.create({
  PokemonListContainerStyles: {
    display: "flex",
    flex: 1
  },
  loading: {
    display: "flex",
    flex: 1,
    flexDirection: "column",
    justifyContent: "center"
  }
});
